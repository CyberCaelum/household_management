决定使用openim的用户登录回调来接受客服登录的信息，登录后将客服id存入redis维护的“在线客服”set中，当接受到openim的
下线回调的时候从set中删除对应的客服id，这样不需要维持心跳，openim中内置心跳等。
使用负载均衡的算法来分配客服，在客服的信息中使用键值对保存客服目前对应的会话个数。
当创建争议或者需要客服的时候，将会话信息存储在redis中，使用键值对
设置过期时间，并设置一个单独的心跳机制，让前端发送信息保持会话
设置主动结束会话的接口，主动结束会话
客服离线时结束所有会话和下线。